/**
 * Durable Object implementation has been moved to an external Worker service.
 *
 * This package now acts as a client only and must not define `extends DurableObject`
 * classes locally.
 */
export {};
